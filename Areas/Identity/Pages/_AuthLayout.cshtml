@{
    Layout = "/Views/Shared/_Layout.cshtml";
}
@* BRAND LOGO *@
@* LOG IN AND REGISTER MENU *@
<div class="row justify-content-center">
    <div class="text-center">
        <img id="main-logo-login-logout" class="img-fluid" src="~/images/logo-verion.png">
    </div>
    <div class="col-5 mt-5">
        <div class="card login-logout-card">
            <div class="card-header">
                <ul class="nav nav-tabs card-header-tabs border border-0">
                    <li class="nav-item border border-0">
                        <a class="nav-link border border-0" href="/Identity/Account/Login">Log In</a>
                    </li>
                    <li class="nav-item border border-0">
                        <a class="nav-link border border-0" href="/Identity/Account/Register">Register</a>
                    </li>
                </ul>
            </div>
            <div class="card-body">
                <div class="row justify-content-center">
                    <div class="">
                        @RenderBody()
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@* SCRIPTS *@
@section Scripts{
    @await RenderSectionAsync("Scripts", required: false)
    <script>
        $(function () {
            const currentPage = location.pathname;
            $('.nav-tabs li a').each(function () {
                const $this = $(this);
                if (currentPage.toLowerCase().indexOf($this.attr('href').toLowerCase()) !== -1) {
                    $this.addClass('active');
                }
            })
        })
    </script>
}